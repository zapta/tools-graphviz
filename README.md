# APIO Graphviz

This repository contains the graphviz packages in a format that is loadeable
by the apio system. Installation and uninstallation of the package is done 
using the ``apio install`` command.

## Creating a new version
Edit the versions in ``build.sh``, VERSION, and VERSION_DEV as needed and run ``build_all.sh``.


